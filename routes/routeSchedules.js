const express = require('express');
const router = express.Router();
const schedulesCtrl = require('../controllers/ctrlSchedules');

// router.get('/performers/new', performersCtrl.new);
// router.post('/performers', performersCtrl.create);

// // http://localhost:3000/movies/6305155d63b7f5eefbf7b696/performers
// router.post('/movies/:id/performers', performersCtrl.addToCast);

module.exports = router;